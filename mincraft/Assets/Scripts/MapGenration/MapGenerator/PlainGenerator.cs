using UnityEngine;

namespace MapGenerator {
    /*public class PlainGenerator: HillsGenrator {
        
        public Block[,,] Generate(MapGenerationArgs pArgs, Vector3 pOrigin) {

            var args = pArgs;
            args.HeightLimit /= 2;
            var result = base.Generate(args, pOrigin);
            return result;
        }
    }*/
}